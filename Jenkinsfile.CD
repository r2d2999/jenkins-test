pipeline{
    agent any
    stages{
        stage('Deploy simulado'){
            steps {
                echo "Deploy simulado a produccion!"
                echo "Artifact: ${params.ARTIFACT_VERSION}"
            }
        }
    }
}